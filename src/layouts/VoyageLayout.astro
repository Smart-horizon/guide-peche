---
/**
 * VoyageLayout.astro
 * Layout dynamique partagé pour toutes les pages voyage.
 * Itère sur toutes les sections du pagebuilder dans l'ordre Sanity.
 * Utilise le CSS voy-* pour préserver le design voyage original.
 */
import BaseLayout from './BaseLayout.astro'
import { stegaClient } from '../lib/sanity.js'
import { stegaClean } from '@sanity/client/stega'
import imageUrlBuilder from '@sanity/image-url'

interface Props {
  doc: any
}

const { doc } = Astro.props
const pb = (doc?.pagebuilder || []) as any[]

const builder = imageUrlBuilder(stegaClient)
const urlFor  = (source: any) => builder.image(source)

// ── SEO ───────────────────────────────────────────────────────────────────────
const heroSection = pb.find((s: any) => s._type === 'sectionHero')
const pageTitle   = doc?.seoTitle || heroSection?.titre || doc?.title || 'Voyage de pêche'
const pageDesc    = doc?.seoDescription || ''

// ── Helpers ───────────────────────────────────────────────────────────────────
const bgClass: Record<string,string> = { white:'section--white', sand:'section--sand', dark:'section--dark' }
const bg = (fond: string) => bgClass[stegaClean(fond) ?? ''] ?? 'section--white'

function blocks(content: any[]): string {
  if (!content?.length) return ''
  return content.map((b: any) => {
    if (b._type !== 'block') return ''
    const text = (b.children || []).map((s: any) => {
      let t = (s.text || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')
      if (s.marks?.includes('strong')) t = `<strong>${t}</strong>`
      if (s.marks?.includes('em'))     t = `<em>${t}</em>`
      return t
    }).join('')
    const tags: Record<string,string> = { h2:'h2', h3:'h3', blockquote:'blockquote', normal:'p' }
    const el = tags[b.style] || 'p'
    return `<${el}>${text}</${el}>`
  }).join('')
}

// Détermine le variant de sectionProgrammeCartes
// L'IMAGE détermine le style visuel ; les colonnes déterminent la grille
// Avec image + 1/2 cols → photo-gauche (photo à gauche, cards à droite)
// Avec image + 3+ cols  → photo-haut (photo en haut, grille en dessous)
// Sans image + 1 col    → texte-cartes (texte à gauche, cards empilées à droite)
// Sans image (2/3/4)    → materiel (grille unifiée, style cards équipement)
function cartesVariant(s: any): 'photo-gauche' | 'photo-haut' | 'texte-cartes' | 'materiel' {
  const cols   = parseInt(stegaClean(s.colonnes) || '3')
  const hasImg = !!s.image?.asset
  if (hasImg && cols <= 2) return 'photo-gauche'
  if (hasImg)              return 'photo-haut'
  if (!hasImg && cols === 1) return 'texte-cartes'
  return 'materiel'
}

// Carousel ID unique par galerie
let carouselCount = 0
---

<BaseLayout title={`${pageTitle} — Jean-Baptiste Vidal`} description={pageDesc}>

{pb.map((s: any) => (
<>

{/* ═══════════════ HERO ═══════════════ */}
{s._type === 'sectionHero' && (() => {
  const heroBg = s.image?.asset
    ? `background-image:url('${urlFor(s.image).width(1600).height(900).url()}')`
    : `background-color:var(--color-ocean)`
  return (
    <section class="voy-hero">
      <div class="voy-hero__bg" style={heroBg}></div>
      <div class="voy-hero__overlay"></div>
      <div class="voy-hero__content container">
        {s.eyebrow && <p class="voy-eyebrow">{s.eyebrow}</p>}
        <h1>{s.titre}<br/><span class="voy-hero__sub">{s.sousTitre}</span></h1>
        <div class="voy-hero__cta">
          <a href={s.btnReserverLien || '/contact'} class="btn-prim">{s.btnReserverTexte || 'Réserver'}</a>
          <a href="tel:0687303456" class="btn-ghost">{s.btnTelTexte || '06 87 30 34 56'}</a>
        </div>
      </div>
      <div class="voy-hero__scroll"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="22" height="22"><path d="M12 5v14M5 12l7 7 7-7"/></svg></div>
    </section>
  )
})()}

{/* ═══════════════ STATS ═══════════════ */}
{s._type === 'sectionStats' && (
  <div class={`voy-stats ${bg(s.fond)}`}>
    {s.stats?.map((st: any) => (
      <div class="voy-stats__item">
        <span class="voy-stats__num">{st.nombre}</span>
        <span class="voy-stats__label">{st.label}</span>
      </div>
    ))}
  </div>
)}

{/* ═══════════════ INTRO + INFO CARD ═══════════════ */}
{s._type === 'sectionIntro' && (
  <section class={`section ${bg(s.fond) || 'section--white'}`}>
    <div class="container voy-intro">
      <div class="voy-intro__text">
        {s.eyebrow && <p class="eyebrow">{s.eyebrow}</p>}
        {s.titre   && <h2 class="voy-intro__h2">{s.titre}</h2>}
        <div set:html={blocks(s.texte)} />
      </div>
      <aside class="voy-intro__card">
        {s.saison  && <><p class="card-label">Saison</p><p class="card-val">{s.saison}</p></>}
        {s.tarif   && <><p class="card-label">Tarifs</p><p class="card-val">{s.tarif}</p></>}
        {s.duree   && <><p class="card-label">Format</p><p class="card-val">{s.duree}</p></>}
        {s.lignesSupp?.map((l: any) => l.label && (
          <><p class="card-label">{l.label}</p><p class="card-val">{l.valeur}</p></>
        ))}
        {s.boutons?.map((btn: any) => (
          <a href={btn.lien} class="card-cta">{btn.texte}</a>
        ))}
      </aside>
    </div>
  </section>
)}

{/* ═══════════════ TEXTE + IMAGE ═══════════════ */}
{s._type === 'sectionTexteImage' && (() => {
  const imgUrl = s.image?.asset ? urlFor(s.image).width(700).height(520).fit('crop').url() : null
  const pos    = stegaClean(s.imagePosition) || 'right'
  return (
    <section class={`section ${bg(s.fond) || 'section--white'}`}>
      <div class={`container voy-lodge-solo${pos === 'left' ? ' voy-lodge-solo--rev' : ''}`}>
        {imgUrl && (
          <div class="voy-lodge-solo__img">
            <img src={imgUrl} alt={s.image?.alt || s.titre || ''} loading="lazy" />
          </div>
        )}
        <div class="voy-lodge-solo__text">
          {s.eyebrow && <p class="eyebrow">{s.eyebrow}</p>}
          {s.titre   && <h2>{s.titre}</h2>}
          <div set:html={blocks(s.texte)} />
        </div>
      </div>
    </section>
  )
})()}

{/* ═══════════════ GRILLE DE CARTES ═══════════════ */}
{s._type === 'sectionProgrammeCartes' && (() => {
  const variant = cartesVariant(s)
  const imgUrl  = s.image?.asset ? urlFor(s.image).width(600).height(520).fit('crop').url() : null

  // ── Variante SAISONS (dark + image + 1/2 cols) ────────────────────────
  if (variant === 'photo-gauche') return (
    <section class={`section ${bg(s.fond) || 'section--dark'}`}>
      <div class={`container${imgUrl ? ' voy-saison-grid' : ''}`}>
        {imgUrl && (
          <div class="voy-saison-img">
            <img src={imgUrl} alt={s.image?.alt || ''} loading="lazy" />
          </div>
        )}
        <div class="voy-saison-text">
          {s.eyebrow && <p class={bg(s.fond) === 'section--dark' ? 'eyebrow-light' : 'eyebrow'}>{s.eyebrow}</p>}
          <h2 class={bg(s.fond) === 'section--dark' ? 'light' : ''}>{s.titre}</h2>
          {s.intro && (s.intro as string).split('\n\n').filter(Boolean).map((para: string) => <p>{para}</p>)}
          <div class={`voy-saison-cards voy-saison-cards--${stegaClean(s.colonnes)||'2'}`}>
            {s.items?.map((item: any) => (
              <div class="voy-saison-card">
                <h3>{item.titre}</h3>
                {item.sousTitre && <p class="voy-saison-card__sub">{item.sousTitre}</p>}
                {item.description && <p>{item.description}</p>}
              </div>
            ))}
          </div>
          {s.note && <p class="voy-saison-note">{s.note}</p>}
          {stegaClean(s.btnTexte) && (stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)) && (
            <div class="voy-materiel-download">
              <a href={stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)} class="btn-download" target="_blank" rel="noopener" download={!!s.btnFichier?.asset?.url}>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="18" height="18"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
                {stegaClean(s.btnTexte)}
              </a>
            </div>
          )}
        </div>
      </div>
    </section>
  )

  // ── Variante ESPÈCES (dark + image + 3 cols) ──────────────────────────
  if (variant === 'photo-haut') return (
    <section class={`section ${bg(s.fond) || 'section--dark'}`}>
      <div class="container">
        {s.eyebrow && <p class={bg(s.fond) === 'section--dark' ? 'eyebrow-light' : 'eyebrow'}>{s.eyebrow}</p>}
        <h2 class={bg(s.fond) === 'section--dark' ? 'light' : ''}>{s.titre}</h2>
        {(imgUrl || s.intro) && (
          <div class={imgUrl ? 'voy-fish-intro' : 'voy-fish-intro voy-fish-intro--no-img'}>
            {imgUrl && <img src={imgUrl} alt={s.image?.alt || ''} loading="lazy" class="voy-fish-hero-img" />}
            {s.intro && <p>{s.intro}</p>}
          </div>
        )}
        <div class={`voy-fish-trio voy-fish-trio--${stegaClean(s.colonnes)||'3'}`}>
          {s.items?.map((item: any) => (
            <div class="voy-fish-species">
              <h3>{item.titre}</h3>
              {item.sousTitre && <p class="voy-fish-tag">{item.sousTitre}</p>}
              {item.description && <p>{item.description}</p>}
            </div>
          ))}
        </div>
        {s.note && <p class="voy-saison-note" style="margin-top:2rem">{s.note}</p>}
        {stegaClean(s.btnTexte) && (stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)) && (
          <div class="voy-materiel-download">
            <a href={stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)} class="btn-download" target="_blank" rel="noopener" download={!!s.btnFichier?.asset?.url}>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="18" height="18"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
              {stegaClean(s.btnTexte)}
            </a>
          </div>
        )}
      </div>
    </section>
  )

  // ── Variante MATÉRIEL (2/3/4 cols sans image) ────────────────────────
  if (variant === 'materiel') return (
    <section class={`section ${bg(s.fond) || 'section--white'}`}>
      <div class="container">
        {s.eyebrow && <p class={bg(s.fond) === 'section--dark' ? 'eyebrow-light' : 'eyebrow'}>{s.eyebrow}</p>}
        {s.titre   && <h2 class={bg(s.fond) === 'section--dark' ? 'light' : ''}>{s.titre}</h2>}
        {s.intro   && <p class={`section-intro${bg(s.fond) === 'section--dark' ? ' section-intro--light' : ''}`}>{s.intro}</p>}
        <div class={`voy-materiel-grid voy-materiel-grid--${stegaClean(s.colonnes)||'3'}`}>
          {s.items?.map((item: any) => (
            <div class="voy-materiel-card">
              <div class="voy-materiel-num">{item.titre}</div>
              {item.sousTitre  && <h3>{item.sousTitre}</h3>}
              {item.description && <p>{item.description}</p>}
            </div>
          ))}
        </div>
        {stegaClean(s.btnTexte) && (stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)) && (
          <div class="voy-materiel-download">
            <a href={stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)} class="btn-download" target="_blank" rel="noopener" download={!!s.btnFichier?.asset?.url}>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="18" height="18"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
              {stegaClean(s.btnTexte)}
            </a>
          </div>
        )}
        {s.note && <p class="voy-saison-note" style="margin-top:2rem">{s.note}</p>}
      </div>
    </section>
  )

  // ── Variante TEXTE + CARTES (sans image, 1 col — texte gauche, cartes droite) ─
  if (variant === 'texte-cartes') return (
    <section class={`section ${bg(s.fond) || 'section--white'}`}>
      <div class="container voy-texte-cartes">
        <div class="voy-texte-cartes__left">
          {s.eyebrow && <p class={bg(s.fond) === 'section--dark' ? 'eyebrow-light' : 'eyebrow'}>{s.eyebrow}</p>}
          {s.titre   && <h2 class={bg(s.fond) === 'section--dark' ? 'light' : ''}>{s.titre}</h2>}
          {s.intro   && (s.intro as string).split('\n\n').filter(Boolean).map((para: string) => <p>{para}</p>)}
        </div>
        <div class="voy-texte-cartes__right">
          {s.items?.map((item: any) => (
            <div class="voy-tc-card">
              <h3>{item.titre}</h3>
              {item.sousTitre  && <p class="voy-tc-card__sub">{item.sousTitre}</p>}
              {item.description && <p class="voy-tc-card__desc">{item.description}</p>}
            </div>
          ))}
        </div>
      </div>
    </section>
  )

  // ── Variante GÉNÉRIQUE ────────────────────────────────────────────────
  return (
    <section class={`section ${bg(s.fond) || 'section--sand'}`}>
      <div class="container">
        {s.eyebrow && <p class="eyebrow">{s.eyebrow}</p>}
        {s.titre   && <h2>{s.titre}</h2>}
        {s.intro   && <p class="section-intro">{s.intro}</p>}
        <div class={`voy-generic-cards voy-generic-cards--${stegaClean(s.colonnes)||'3'}`}>
          {s.items?.map((item: any) => (
            <div class="voy-generic-card">
              {item.sousTitre && <p class="voy-generic-card__sub">{item.sousTitre}</p>}
              <h3>{item.titre}</h3>
              {item.description && <p>{item.description}</p>}
            </div>
          ))}
        </div>
        {s.note && <p class="voy-saison-note" style="margin-top:2rem">{s.note}</p>}
        {stegaClean(s.btnTexte) && (stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)) && (
          <div style="margin-top:2rem">
            <a href={stegaClean(s.btnFichier?.asset?.url) || stegaClean(s.btnLien)} class="btn-download" target="_blank" rel="noopener" download={!!s.btnFichier?.asset?.url}>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="18" height="18"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
              {stegaClean(s.btnTexte)}
            </a>
          </div>
        )}
      </div>
    </section>
  )
})()}

{/* ═══════════════ PROGRAMME ═══════════════ */}
{s._type === 'sectionProgramme' && (
  <section class={`section ${bg(s.fond) || 'section--sand'}`}>
    <div class="container">
      {s.eyebrow && <p class="eyebrow">{s.eyebrow}</p>}
      {s.titre   && <h2>{s.titre}</h2>}
      <div class="voy-programme">
        {s.etapes?.map((etape: any) => (
          <div class="voy-prog-item">
            <span class="voy-prog-day">{etape.tag || ''}</span>
            <div><strong>{etape.titre}</strong>{etape.texte?.[0]?.children?.[0]?.text ? ` : ${etape.texte[0].children[0].text}` : ''}</div>
          </div>
        ))}
      </div>
    </div>
  </section>
)}

{/* ═══════════════ GALERIE ═══════════════ */}
{s._type === 'sectionGalerie' && (() => {
  const cid = `carousel-${++carouselCount}`
  return (
    <section class="gallery-section">
      <div class="voy-carousel" id={cid} data-carousel>
        <div class="voy-carousel__track">
          {s.photos?.map((p: any, i: number) => (
            <div class="voy-carousel__slide">
              <img src={urlFor(p).width(900).height(600).fit('crop').url()} alt={p.alt || `Photo ${i+1}`} loading={i === 0 ? 'eager' : 'lazy'} />
            </div>
          ))}
        </div>
        <button class="voy-carousel__btn voy-carousel__btn--prev" aria-label="Précédent"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="20" height="20"><polyline points="15 18 9 12 15 6"/></svg></button>
        <button class="voy-carousel__btn voy-carousel__btn--next" aria-label="Suivant"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="20" height="20"><polyline points="9 18 15 12 9 6"/></svg></button>
        <div class="voy-carousel__counter">1 / {s.photos?.length || 0}</div>
      </div>
    </section>
  )
})()}

{/* ═══════════════ DATES ═══════════════ */}
{s._type === 'sectionDates' && (
  <section class={`section ${bg(s.fond) || 'section--white'}`}>
    <div class="container voy-dates">
      {s.eyebrow && <p class="eyebrow">{s.eyebrow}</p>}
      {s.titre   && <h2>{s.titre}</h2>}
      {s.intro   && <p class="section-intro">{s.intro}</p>}
      <div class="voy-dates-grid">
        {s.sejours?.map((sej: any, i: number) => (
          <div class="voy-date-card">
            <span class="voy-date-badge">Semaine {i + 1}</span>
            <h3>{sej.dates}</h3>
            {sej.description && <p>{sej.description}</p>}
            {sej.prix         && <p class="voy-date-prix">{sej.prix}</p>}
            <a href="/contact" class="btn-prim btn-sm">Se renseigner</a>
          </div>
        ))}
      </div>
    </div>
  </section>
)}

{/* ═══════════════ CHOIX (LODGES) ═══════════════ */}
{s._type === 'sectionChoix' && (() => {
  return (
    <section class={`section ${bg(s.fond) || 'section--sand'}`}>
      <div class="container">
        {s.eyebrow && <p class={bg(s.fond) === 'section--dark' ? 'eyebrow-light' : 'eyebrow'}>{s.eyebrow}</p>}
        {s.titre   && <h2 class={bg(s.fond) === 'section--dark' ? 'light' : ''}>{s.titre}</h2>}
        {s.intro   && <p class="section-intro">{s.intro}</p>}
        {s.options?.length > 0 && (
          <div class="voy-choix-grid">
            {s.options.map((opt: any) => {
              const imgUrl = opt.image?.asset ? urlFor(opt.image).width(700).height(480).fit('crop').url() : null
              return (
                <div class="voy-choix-item">
                  {imgUrl && (
                    <div class="voy-choix-img">
                      <img src={imgUrl} alt={opt.image?.alt || opt.titre || ''} loading="lazy" />
                    </div>
                  )}
                  <div class="voy-choix-body">
                    {opt.numero && <div class="voy-choix-num">{opt.numero}</div>}
                    {opt.titre  && <h3>{opt.titre}</h3>}
                    {opt.tag    && <span class="voy-choix-tag">{opt.tag}</span>}
                    {opt.description && <p>{opt.description}</p>}
                  </div>
                </div>
              )
            })}
          </div>
        )}
        {s.galerie?.length > 0 && (
          <div class="voy-choix-gallery">
            {s.galerie.map((img: any, i: number) => {
              const gUrl = img.asset ? urlFor(img).width(800).height(560).fit('crop').url() : null
              return gUrl ? (
                <div class="voy-choix-gallery__item">
                  <img src={gUrl} alt={img.alt || `Photo ${i+1}`} loading="lazy" />
                </div>
              ) : null
            })}
          </div>
        )}
      </div>
    </section>
  )
})()}

{/* ═══════════════ INCLUS / NON INCLUS ═══════════════ */}
{s._type === 'sectionProgrammeTexte' && (
  <section class={`section ${bg(s.fond) || 'section--dark'}`}>
    <div class="container voy-inclus-grid">
      {s.colonnes?.map((col: any, ci: number) => (
        <div>
          <p class={ci === 0 ? 'eyebrow-light' : 'eyebrow-light-muted'}>{col.label}</p>
          <h2 class="light">{ci === 0 ? 'Ce qui est inclus' : 'Non inclus'}</h2>
          <ul class={ci === 0 ? 'voy-check-list' : 'voy-cross-list'}>
            {col.items?.map((item: any) => (
              <li>
                {ci === 0
                  ? <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="14" height="14"><polyline points="20 6 9 17 4 12"/></svg>
                  : <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="13" height="13"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                }
                {item.texte}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </section>
)}

{/* ═══════════════ TEXTE SIMPLE (pratique, etc.) ═══════════════ */}
{s._type === 'sectionTexte' && (
  <section class={`section ${bg(s.fond) || 'section--sand'}`}>
    <div class="container voy-pratique">
      <div class="voy-pratique__icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" width="32" height="32"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      </div>
      <div class="prose" set:html={blocks(s.texte)} />
    </div>
  </section>
)}

{/* ═══════════════ CTA ═══════════════ */}
{s._type === 'sectionCta' && (
  <section class="section section--dark voy-cta-final">
    <div class="container voy-cta-final__inner">
      <div>
        <h2>{s.titre}</h2>
        {s.texte && <p>{s.texte}</p>}
      </div>
      <div class="voy-cta-final__btns">
        <a href={s.btn1Lien || '/contact'} class="btn-prim">{s.btn1Texte || 'Me contacter'}</a>
        {s.btn2Texte && <a href={s.btn2Lien || 'tel:0687303456'} class="btn-ghost-light">{s.btn2Texte}</a>}
      </div>
    </div>
  </section>
)}

</>
))}

</BaseLayout>

<script>
document.querySelectorAll('[data-carousel]').forEach(carousel => {
  const track   = carousel.querySelector('.voy-carousel__track') as HTMLElement
  const counter = carousel.querySelector('.voy-carousel__counter')
  if (!track) return
  const slides = track.querySelectorAll('.voy-carousel__slide')
  const total  = slides.length
  let current  = 0
  let timer: ReturnType<typeof setInterval> | null = null

  const startAutoplay = () => { if (timer) return; timer = setInterval(() => go(current + 1), 4500) }
  const stopAutoplay  = () => { if (timer) { clearInterval(timer); timer = null } }
  const restartAutoplay = () => { stopAutoplay(); setTimeout(startAutoplay, 6000) }

  const slideW   = () => (slides[0] as HTMLElement).offsetWidth
  const visibleN = () => window.innerWidth <= 640 ? 1 : window.innerWidth <= 900 ? 2 : 3
  const calcOffset = (idx: number) => {
    const w = slideW(), g = 3, n = visibleN()
    const raw = n < 3 ? idx * (w + g) : (idx - 1) * (w + g)
    return Math.max(0, Math.min(Math.max(0, (total - n) * (w + g)), raw))
  }
  const go = (idx: number) => {
    current = (idx + total) % total
    track.style.transform = `translateX(-${calcOffset(current)}px)`
    if (counter) counter.textContent = `${current + 1} / ${total}`
  }

  window.addEventListener('resize', () => {
    track.style.transition = 'none'
    track.style.transform  = `translateX(-${calcOffset(current)}px)`
    requestAnimationFrame(() => { track.style.transition = '' })
  })

  carousel.querySelector('.voy-carousel__btn--prev')?.addEventListener('click', () => { go(current - 1); restartAutoplay() })
  carousel.querySelector('.voy-carousel__btn--next')?.addEventListener('click', () => { go(current + 1); restartAutoplay() })

  let startX = 0
  carousel.addEventListener('touchstart', (e: TouchEvent) => { startX = e.changedTouches[0].clientX }, { passive: true })
  carousel.addEventListener('touchend',   (e: TouchEvent) => {
    const dx = e.changedTouches[0].clientX - startX
    if (Math.abs(dx) > 40) { go(current + (dx < 0 ? 1 : -1)); restartAutoplay() }
  }, { passive: true })

  carousel.addEventListener('mouseenter', stopAutoplay)
  carousel.addEventListener('mouseleave', startAutoplay)
  startAutoplay()
})
</script>

<style>
/* ── Hero ── */
.voy-hero { position:relative;min-height:100vh;display:flex;align-items:flex-end;padding-bottom:5rem;color:white;overflow:hidden; }
.voy-hero__bg { position:absolute;inset:0;background-size:cover;background-position:center;transition:transform 8s ease; }
.voy-hero:hover .voy-hero__bg { transform:scale(1.03); }
.voy-hero__overlay { position:absolute;inset:0;background:linear-gradient(to top,rgba(5,18,26,.92) 0%,rgba(5,18,26,.55) 50%,rgba(5,18,26,.2) 100%); }
.voy-hero__content { position:relative;z-index:1;max-width:700px;margin-left:clamp(2rem,8vw,120px);margin-right:auto; }
.voy-eyebrow { font-size:.68rem;letter-spacing:.2em;text-transform:uppercase;color:var(--color-ocean-light);font-weight:500;margin-bottom:1rem; }
.voy-hero h1 { font-size:clamp(3rem,8vw,6rem);color:white;margin:0 0 1.5rem;line-height:1; }
.voy-hero__sub { font-size:clamp(1.2rem,3vw,2rem);font-weight:300;opacity:.7;display:block; }
.voy-hero__cta { display:flex;gap:1rem;flex-wrap:wrap; }
.voy-hero__scroll { position:absolute;bottom:2rem;left:50%;transform:translateX(-50%);color:rgba(255,255,255,.4);animation:bounce 2s infinite; }
@keyframes bounce { 0%,100%{transform:translateX(-50%) translateY(0)}50%{transform:translateX(-50%) translateY(6px)} }

/* ── Stats ── */
.voy-stats { display:grid;grid-template-columns:repeat(4,1fr);background:var(--color-ocean); }
.voy-stats__item { padding:2rem 1.5rem;text-align:center;border-right:1px solid rgba(255,255,255,.08);display:flex;flex-direction:column;gap:.4rem; }
.voy-stats__item:last-child { border-right:none; }
.voy-stats__num { font-family:var(--font-serif);font-size:clamp(1.4rem,3vw,2rem);color:var(--color-ocean-light);font-weight:600;line-height:1; }
.voy-stats__label { font-size:.7rem;text-transform:uppercase;letter-spacing:.12em;color:rgba(255,255,255,.5); }

/* ── Sections communes ── */
.section { padding:5rem 0; }
.section--white { background:white; }
.section--sand  { background:var(--color-sand); }
.section--dark  { background:#07181f; }
.eyebrow { font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;color:var(--color-ocean-main);font-weight:500;margin-bottom:.5rem; }
.eyebrow-light { font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;color:var(--color-ocean-light);font-weight:500;margin-bottom:.5rem; }
.eyebrow-light-muted { font-size:.68rem;letter-spacing:.18em;text-transform:uppercase;color:rgba(255,255,255,.4);font-weight:500;margin-bottom:.5rem; }
h2.light { color:white;margin:.4rem 0 1.5rem; }
.section-intro { color:var(--color-text-muted,#555);font-size:.95rem;line-height:1.75;max-width:760px;margin-bottom:3rem; }

/* ── Intro ── */
.voy-intro { display:grid;grid-template-columns:1fr 280px;gap:5rem;align-items:start; }
.voy-intro__h2 { margin:.4rem 0 1.5rem;color:var(--color-text); }
.voy-intro__text h2 { margin:.4rem 0 1.5rem; }
.voy-intro__text p { line-height:1.8;color:var(--color-text-muted,#555);margin-bottom:1.1rem; }
.voy-intro__card { background:var(--color-sand);border-radius:6px;padding:1.75rem;border-left:3px solid var(--color-ocean-main);position:sticky;top:5rem; }
.card-label { font-size:.62rem;text-transform:uppercase;letter-spacing:.12em;color:var(--color-ocean-main);font-weight:700;margin-top:1.1rem; }
.card-label:first-child { margin-top:0; }
.card-val { font-size:.88rem;color:var(--color-text-muted,#555);line-height:1.5;margin-bottom:0; }
.card-cta { display:block;margin-top:1.5rem;text-align:center;background:var(--color-ocean-main);color:white;padding:.7rem 1.2rem;border-radius:4px;font-size:.85rem;font-weight:500;text-decoration:none;transition:background .2s; }
.card-cta:hover { background:#144b72; }

/* ── Texte + Image ── */
.voy-lodge-solo { display:grid;grid-template-columns:1fr 1fr;gap:4rem;align-items:center; }
.voy-lodge-solo--rev { direction:rtl; }
.voy-lodge-solo--rev > * { direction:ltr; }
.voy-lodge-solo__img { border-radius:8px;overflow:hidden;height:420px; }
.voy-lodge-solo__img img { width:100%;height:100%;object-fit:cover;display:block; }
.voy-lodge-solo__text h2 { margin:.4rem 0 1.5rem; }
.voy-lodge-solo__text p { font-size:.9rem;line-height:1.8;color:var(--color-text-muted,#555);margin-bottom:1rem; }
.section--dark .voy-lodge-solo__text p { color:rgba(255,255,255,.7); }

/* ── Saisons ── */
.voy-saison-grid { display:grid;grid-template-columns:380px 1fr;gap:5rem;align-items:start; }
.voy-saison-img { border-radius:8px;overflow:hidden;height:420px;position:sticky;top:5rem; }
.voy-saison-img img { width:100%;height:100%;object-fit:cover;display:block; }
.voy-saison-text h2 { margin:.4rem 0 1.5rem; }
.voy-saison-text p { font-size:.9rem;line-height:1.8;color:rgba(255,255,255,.7);margin-bottom:1rem; }
/* Carte saison — fond dark par défaut, overrides pour fonds clairs ci-dessous */
.voy-saison-card { background:rgba(255,255,255,.08);border-radius:6px;padding:1.25rem;border-top:2px solid var(--color-ocean-light); }
.voy-saison-card h3 { color:var(--color-ocean-light);font-size:.9rem;margin-bottom:.5rem;font-family:var(--font-serif); }
.voy-saison-card p { font-size:.82rem;color:rgba(255,255,255,.65);line-height:1.6;margin:0; }
.voy-saison-note { font-size:.85rem;color:rgba(255,255,255,.55);font-style:italic; }
/* Fonds clairs → carte blanche avec bordure solide visible */
.section--white .voy-saison-card,
.section--sand  .voy-saison-card {
  background: #fff;
  border: 1px solid #b8cdd8;
  border-top: 2px solid var(--color-ocean-main);
  box-shadow: 0 2px 10px rgba(0,0,0,.06);
}

/* ── Saison — colonnes dynamiques ── */
.voy-saison-cards { display:grid;gap:1rem;margin:1.5rem 0; }
.voy-saison-cards--1 { grid-template-columns:1fr; }
.voy-saison-cards--2 { grid-template-columns:1fr 1fr; }
.voy-saison-cards--3 { grid-template-columns:repeat(3,1fr); }
.voy-saison-cards--4 { grid-template-columns:repeat(4,1fr); }
.voy-saison-card__sub { font-size:.72rem;text-transform:uppercase;letter-spacing:.1em;color:rgba(255,255,255,.45);margin-bottom:.4rem; }

/* ── Espèces ── */
.voy-fish-intro { display:grid;grid-template-columns:340px 1fr;gap:3rem;align-items:center;margin:2rem 0 3rem; }
.voy-fish-intro--no-img { grid-template-columns:1fr;max-width:760px; }
.voy-fish-hero-img { width:100%;height:260px;object-fit:cover;border-radius:8px;display:block; }
.voy-fish-intro p { font-size:.95rem;line-height:1.8;color:rgba(255,255,255,.7); }
.voy-fish-trio { display:grid;grid-template-columns:repeat(3,1fr);gap:2rem; }
/* Carte espèces — fond dark par défaut, overrides pour fonds clairs ci-dessous */
.voy-fish-species { background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.08);border-radius:6px;padding:1.75rem; }
/* Fonds clairs → carte blanche avec bordure solide visible */
.section--white .voy-fish-species,
.section--sand  .voy-fish-species {
  background: #fff;
  border: 1px solid #b8cdd8;
  box-shadow: 0 2px 10px rgba(0,0,0,.06);
}
.voy-fish-species h3 { color:var(--color-ocean-light);font-family:var(--font-serif);font-size:1.2rem;margin-bottom:.3rem; }
.voy-fish-tag { font-size:.65rem;text-transform:uppercase;letter-spacing:.12em;color:rgba(255,255,255,.4);margin-bottom:.85rem; }
.voy-fish-species p { font-size:.88rem;line-height:1.75;color:rgba(255,255,255,.65);margin:0; }

/* ── Texte + Cartes (sans image, 1 col — texte gauche, cartes droite) ── */
.voy-texte-cartes { display:grid;grid-template-columns:1fr 280px;gap:5rem;align-items:start; }
.voy-texte-cartes__left h2 { margin:.4rem 0 1.5rem; }
.voy-texte-cartes__left p  { font-size:.9rem;line-height:1.8;color:var(--color-text-muted,#555);margin-bottom:1.1rem; }
.section--dark .voy-texte-cartes__left p { color:rgba(255,255,255,.7); }
.voy-texte-cartes__right { display:flex;flex-direction:column;gap:.75rem;padding-top:.35rem; }
.voy-tc-card { padding:1.25rem 1.5rem;border-left:3px solid var(--color-ocean-main);background:var(--color-sand);border-radius:0 4px 4px 0; }
.section--dark .voy-tc-card { background:rgba(255,255,255,.08);border-left-color:var(--color-ocean-light); }
.voy-tc-card h3 { font-family:var(--font-sans);font-size:.72rem;font-weight:700;text-transform:uppercase;letter-spacing:.12em;color:var(--color-ocean-main);margin-bottom:.4rem; }
.section--dark .voy-tc-card h3 { color:var(--color-ocean-light); }
.voy-tc-card__sub { font-size:.72rem;text-transform:uppercase;letter-spacing:.08em;color:var(--color-ocean-main);opacity:.65;margin-bottom:.25rem; }
.voy-tc-card__desc { font-size:.82rem;line-height:1.55;color:var(--color-text-muted,#555);margin:0; }
.section--dark .voy-tc-card__desc { color:rgba(255,255,255,.6); }

/* ── Matériel (2 / 3 / 4 cols) ── */
.voy-materiel-grid { display:grid;gap:1.5rem;margin-bottom:2.5rem; }
.voy-materiel-grid--2 { grid-template-columns:repeat(2,1fr); }
.voy-materiel-grid--3 { grid-template-columns:repeat(3,1fr); }
.voy-materiel-grid--4 { grid-template-columns:repeat(4,1fr); }

/* Carte matériel — visible sur TOUS les fonds clairs grâce à la bordure */
.voy-materiel-card {
  background: #fff;
  border-radius: 6px;
  padding: 1.5rem;
  border: 1px solid #b8cdd8;
  border-top: 3px solid var(--color-ocean-main);
  box-shadow: 0 2px 10px rgba(0,0,0,.06);
}
.voy-materiel-num { font-family:var(--font-serif);font-size:1.5rem;font-weight:700;color:var(--color-ocean-main);margin-bottom:.4rem; }
.voy-materiel-card h3 { font-size:.9rem;color:var(--color-ocean);margin-bottom:.6rem; }
.voy-materiel-card p { font-size:.82rem;line-height:1.65;color:var(--color-text-muted,#555);margin:0; }

/* Fond dark → textes clairs, fond semi-transparent */
.section--dark .voy-materiel-card { background:rgba(255,255,255,.07);border-color:rgba(255,255,255,.12);border-top-color:var(--color-ocean-light);box-shadow:none; }
.section--dark .voy-materiel-num { color:var(--color-ocean-light); }
.section--dark .voy-materiel-card h3 { color:rgba(255,255,255,.9); }
.section--dark .voy-materiel-card p { color:rgba(255,255,255,.65); }

/* Bouton PDF */
.voy-materiel-download { display:flex;margin-top:2.5rem; }
.section-intro--light { color:rgba(255,255,255,.75); }
.btn-download { display:inline-flex;align-items:center;gap:.6rem;background:var(--color-ocean-main);color:white;padding:.85rem 1.75rem;border-radius:4px;font-weight:500;font-size:.9rem;text-decoration:none;transition:background .2s; }
.btn-download:hover { background:#144b72; }
.section--dark .btn-download { background:white;color:var(--color-ocean-main); }
.section--dark .btn-download:hover { background:rgba(255,255,255,.88); }

/* ── Generic cards ── */
.voy-generic-cards { display:grid;gap:1.5rem;margin-bottom:1.5rem; }
.voy-generic-cards--2 { grid-template-columns:repeat(2,1fr); }
.voy-generic-cards--3 { grid-template-columns:repeat(3,1fr); }
.voy-generic-cards--4 { grid-template-columns:repeat(4,1fr); }
.voy-generic-card { background:white;border-radius:6px;padding:1.5rem;border-top:3px solid var(--color-ocean-main);box-shadow:0 2px 12px rgba(0,0,0,.06); }
.section--dark .voy-generic-card { background:rgba(255,255,255,.07);border-top-color:var(--color-ocean-light); }
.voy-generic-card h3 { color:var(--color-ocean);font-size:1rem;margin-bottom:.5rem; }
.section--dark .voy-generic-card h3 { color:var(--color-ocean-light); }
.voy-generic-card p { font-size:.88rem;line-height:1.7;color:var(--color-text-muted,#555);margin:0; }
.section--dark .voy-generic-card p { color:rgba(255,255,255,.65); }
.voy-generic-card__sub { font-size:.65rem;text-transform:uppercase;letter-spacing:.12em;color:var(--color-ocean-main);font-weight:600;margin-bottom:.3rem; }
.section--dark .voy-generic-card__sub { color:var(--color-ocean-light); }

/* ── Programme ── */
.voy-programme { display:flex;flex-direction:column;gap:1rem;max-width:760px;margin-top:2rem; }
.voy-prog-item { display:flex;gap:1.5rem;align-items:flex-start;padding:1.25rem;background:white;border-radius:6px;box-shadow:0 1px 8px rgba(0,0,0,.06); }
.voy-prog-day { flex-shrink:0;font-family:var(--font-serif);font-size:1rem;font-weight:700;color:var(--color-ocean-main);width:9rem;padding-top:.1rem; }
.voy-prog-item div { font-size:.88rem;line-height:1.7;color:var(--color-text-muted,#555); }

/* ── Galerie ── */
.gallery-section { background:var(--color-ocean);overflow:hidden; }
.voy-carousel { position:relative;overflow:hidden;cursor:grab; }
.voy-carousel:active { cursor:grabbing; }
.voy-carousel__track { display:flex;align-items:stretch;gap:3px;transition:transform .55s cubic-bezier(.4,0,.2,1);will-change:transform; }
.voy-carousel__slide { flex:0 0 calc((100% - 6px)/3);width:calc((100% - 6px)/3);aspect-ratio:3/2;overflow:hidden; }
.voy-carousel__slide img { width:100%;height:100%;object-fit:cover;display:block;user-select:none;-webkit-user-drag:none; }
.voy-carousel__btn { position:absolute;top:50%;transform:translateY(-50%);width:46px;height:46px;border-radius:50%;background:rgba(0,0,0,.45);border:1.5px solid rgba(255,255,255,.2);color:white;cursor:pointer;transition:background .2s;display:flex;align-items:center;justify-content:center;z-index:10;padding:0; }
.voy-carousel__btn:hover { background:rgba(0,0,0,.7);border-color:rgba(255,255,255,.5); }
.voy-carousel__btn--prev { left:1rem; }
.voy-carousel__btn--next { right:1rem; }
.voy-carousel__counter { position:absolute;bottom:.85rem;right:1rem;z-index:10;font-size:.7rem;letter-spacing:.12em;color:rgba(255,255,255,.6);background:rgba(0,0,0,.35);padding:.2rem .55rem;border-radius:2px; }

/* ── Dates ── */
.voy-dates-grid { display:grid;grid-template-columns:1fr 1fr;gap:2rem; }
.voy-date-card { background:var(--color-sand);border-radius:8px;padding:2rem;border-left:3px solid var(--color-ocean-main);display:flex;flex-direction:column;gap:.85rem; }
.voy-date-badge { font-size:.65rem;text-transform:uppercase;letter-spacing:.12em;color:var(--color-ocean-main);font-weight:700; }
.voy-date-card h3 { font-family:var(--font-serif);font-size:1.3rem;color:var(--color-ocean);margin:0; }
.voy-date-card p { font-size:.88rem;line-height:1.65;color:var(--color-text-muted,#555);margin:0; }
.voy-date-prix { font-weight:600;color:var(--color-ocean-main) !important; }

/* ── Inclus ── */
.voy-inclus-grid { display:grid;grid-template-columns:1fr 1fr;gap:5rem; }
.voy-check-list,.voy-cross-list { list-style:none;padding:0;margin:0;display:flex;flex-direction:column;gap:.85rem; }
.voy-check-list li,.voy-cross-list li { display:flex;align-items:flex-start;gap:.7rem;font-size:.9rem;line-height:1.55; }
.voy-check-list li { color:rgba(255,255,255,.8); }
.voy-check-list li svg { flex-shrink:0;color:var(--color-ocean-light);margin-top:.22rem; }
.voy-cross-list li { color:rgba(255,255,255,.45); }
.voy-cross-list li svg { flex-shrink:0;color:rgba(255,255,255,.25);margin-top:.22rem; }

/* ── Pratique ── */
.voy-pratique { display:flex;gap:2.5rem;align-items:flex-start;max-width:820px; }
.voy-pratique__icon { color:var(--color-ocean-main);flex-shrink:0;padding-top:.2rem; }
.voy-pratique .prose p { font-size:.9rem;line-height:1.75;color:var(--color-text-muted,#555);margin-bottom:.8rem; }
.voy-pratique .prose h3 { color:var(--color-ocean);margin-bottom:.85rem;font-size:1.15rem; }

/* ── CTA ── */
.voy-cta-final__inner { display:flex;align-items:center;justify-content:space-between;gap:2rem;flex-wrap:wrap; }
.voy-cta-final__inner h2 { color:white;margin-bottom:.5rem; }
.voy-cta-final__inner > div:first-child p { color:rgba(255,255,255,.6); }
.voy-cta-final__btns { display:flex;gap:1rem;flex-wrap:wrap; }

/* ── Boutons ── */
.btn-prim { display:inline-block;background:var(--color-ocean-main);color:white;padding:.85rem 2rem;border-radius:4px;font-weight:500;font-size:.95rem;text-decoration:none;transition:background .2s; }
.btn-prim:hover { background:#144b72; }
.btn-sm { padding:.6rem 1.25rem;font-size:.85rem; }
.btn-ghost { display:inline-block;border:1.5px solid rgba(255,255,255,.35);color:white;padding:.85rem 2rem;border-radius:4px;font-weight:500;font-size:.95rem;text-decoration:none;transition:border-color .2s; }
.btn-ghost:hover { border-color:white; }
.btn-ghost-light { display:inline-block;border:1.5px solid rgba(255,255,255,.35);color:white;padding:.85rem 2rem;border-radius:4px;font-weight:500;font-size:.95rem;text-decoration:none;transition:border-color .2s; }
.btn-ghost-light:hover { border-color:white; }

/* ── Prose ── */
.prose p { line-height:1.8;color:var(--color-text-muted,#555);margin-bottom:1rem; }
.prose h2 { font-family:var(--font-serif);color:var(--color-text);margin:.4rem 0 1.5rem; }
.prose h3 { font-family:var(--font-serif);color:var(--color-ocean);margin:.4rem 0 1rem; }

/* ══════════════════════════════════════════════════════════════
   OVERRIDES FONDS CLAIRS (white / sand)
   Les styles de base sont optimisés pour section--dark.
   Ces règles les adaptent pour les fonds blancs et sable.
   ══════════════════════════════════════════════════════════════ */

/* ── Textes généraux sur fonds clairs ── */
.section--white h2, .section--sand h2 { color: var(--color-text); }
.section--white p,  .section--sand p  { color: var(--color-text-muted,#555); }

/* ── Style A — saison cards sur fonds clairs ── */
.section--white .voy-saison-text p,
.section--sand  .voy-saison-text p    { color: var(--color-text-muted,#555); }

/* (saison card overrides moved to base section above) */
.section--white .voy-saison-card h3,
.section--sand  .voy-saison-card h3  { color: var(--color-ocean); }
.section--white .voy-saison-card p,
.section--sand  .voy-saison-card p   { color: var(--color-text-muted,#555); }
.section--white .voy-saison-card__sub,
.section--sand  .voy-saison-card__sub { color: var(--color-ocean-main); opacity:.65; }
.section--white .voy-saison-note,
.section--sand  .voy-saison-note     { color: var(--color-text-muted,#555); font-style:italic; }

/* ── Style B — fish species cards sur fonds clairs ── */
.section--white .voy-fish-intro p,
.section--sand  .voy-fish-intro p    { color: var(--color-text-muted,#555); }

/* (fish species overrides moved to base section above) */
.section--white .voy-fish-species h3,
.section--sand  .voy-fish-species h3 { color: var(--color-ocean); }
.section--white .voy-fish-tag,
.section--sand  .voy-fish-tag        { color: var(--color-ocean-main); opacity:.65; }
.section--white .voy-fish-species p,
.section--sand  .voy-fish-species p  { color: var(--color-text-muted,#555); }

/* ── Cartes génériques sur fonds clairs ── */
.section--white .voy-generic-card { background: var(--color-sand); box-shadow:none; }
.section--sand  .voy-generic-card { background: white; }
.section--white .voy-generic-card h3,
.section--sand  .voy-generic-card h3 { color: var(--color-ocean); }
.section--white .voy-generic-card p,
.section--sand  .voy-generic-card p  { color: var(--color-text-muted,#555); }
.section--white .voy-generic-card__sub { color: var(--color-ocean-main); }
.section--sand  .voy-generic-card__sub { color: var(--color-ocean-main); }

/* ── Dates sur fond clair ── */
.section--white .voy-date-card { background: var(--color-sand); }
.section--sand  .voy-date-card { background: white; }

/* ── Programme sur fond clair ── */
.section--white .voy-prog-item { box-shadow: 0 1px 8px rgba(0,0,0,.06); }
.section--sand  .voy-prog-item { background: white; }

/* ── Choix (lodges) ── */
.voy-choix-grid { display:grid;grid-template-columns:1fr 1fr;gap:2.5rem; }
.voy-choix-item { border-radius:8px;overflow:hidden;background:white;box-shadow:0 2px 16px rgba(0,0,0,.08); }
.section--dark .voy-choix-item { background:rgba(255,255,255,.07);box-shadow:none; }
.voy-choix-img { height:240px;overflow:hidden; }
.voy-choix-img img { width:100%;height:100%;object-fit:cover;display:block;transition:transform .5s ease; }
.voy-choix-item:hover .voy-choix-img img { transform:scale(1.04); }
.voy-choix-body { padding:1.75rem; }
.voy-choix-num { font-family:var(--font-serif);font-size:2.5rem;font-weight:700;line-height:1;color:var(--color-ocean-main);opacity:.18;margin-bottom:.5rem; }
.voy-choix-item h3 { font-family:var(--font-serif);font-size:1.25rem;color:var(--color-ocean);margin-bottom:.3rem; }
.section--dark .voy-choix-item h3 { color:var(--color-ocean-light); }
.voy-choix-tag { font-size:.68rem;text-transform:uppercase;letter-spacing:.12em;color:var(--color-ocean-main);display:block;margin-bottom:.9rem; }
.section--dark .voy-choix-tag { color:var(--color-ocean-light); }
.voy-choix-body p { font-size:.88rem;line-height:1.75;color:var(--color-text-muted,#555);margin-bottom:.75rem; }
.section--dark .voy-choix-body p { color:rgba(255,255,255,.65); }
.voy-choix-gallery { display:grid;grid-template-columns:2fr 1fr 1fr;grid-template-rows:280px;gap:6px;margin-top:2rem;border-radius:8px;overflow:hidden; }
.voy-choix-gallery__item { overflow:hidden; }
.voy-choix-gallery__item:first-child { grid-column:1; }
.voy-choix-gallery__item img { width:100%;height:100%;object-fit:cover;display:block;transition:transform .5s; }
.voy-choix-gallery__item:hover img { transform:scale(1.04); }

/* ── Responsive ── */
@media (max-width:960px) {
  .voy-stats { grid-template-columns:repeat(2,1fr); }
  .voy-intro { grid-template-columns:1fr;gap:3rem; }
  .voy-intro__card { position:static; }
  .voy-lodge-solo { grid-template-columns:1fr;gap:2rem;direction:ltr; }
  .voy-lodge-solo--rev { direction:ltr; }
  .voy-lodge-solo__img { height:300px; }
  .voy-saison-grid { grid-template-columns:1fr;gap:3rem; }
  .voy-saison-img { position:static;height:280px; }
  .voy-fish-intro { grid-template-columns:1fr; }
  .voy-fish-trio { grid-template-columns:1fr;gap:1.5rem; }
  .voy-materiel-grid--3,.voy-materiel-grid--4 { grid-template-columns:repeat(2,1fr); }
  .voy-dates-grid { grid-template-columns:1fr; }
  .voy-inclus-grid { grid-template-columns:1fr;gap:3rem; }
  .voy-generic-cards--3,.voy-generic-cards--4 { grid-template-columns:repeat(2,1fr); }
  .voy-choix-grid { grid-template-columns:1fr; }
  .voy-choix-gallery { grid-template-columns:1fr 1fr;grid-template-rows:200px 200px; }
}
@media (max-width:900px) {
  .voy-carousel__slide { flex:0 0 calc((100% - 3px)/2);width:calc((100% - 3px)/2); }
}
@media (max-width:640px) {
  .voy-carousel__slide { flex:0 0 100%;width:100%; }
  .voy-saison-cards { grid-template-columns:1fr; }
  .voy-materiel-grid--2,.voy-materiel-grid--3,.voy-materiel-grid--4,.voy-generic-cards--2,.voy-generic-cards--3,.voy-generic-cards--4 { grid-template-columns:1fr; }
  .voy-cta-final__inner { flex-direction:column;align-items:flex-start; }
  .voy-pratique { flex-direction:column;gap:1rem; }
  .voy-choix-gallery { grid-template-columns:1fr;grid-template-rows:auto; }
  .voy-choix-gallery__item { height:220px; }
}
</style>
